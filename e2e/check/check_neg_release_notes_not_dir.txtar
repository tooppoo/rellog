
mkdir .rellog
exec touch .rellog/release-notes

! exec rellog check
! stdout .
cmp stderr expected.txt

-- expected.txt --
rellog check: FAILED

1 files
1 errors

.rellog/release-notes
  error[release_notes_dir.not_directory]
    release-notes path is not a directory.

    Expected a directory for release-notes, but found a file.
    Remove or rename the file, then create the directory:
      mkdir -p .rellog/release-notes
