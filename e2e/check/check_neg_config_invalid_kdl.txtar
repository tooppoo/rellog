exec sh -c 'git init && git remote add origin https://github.com/tooppoo/rellog'


exec rellog init

! exec rellog check
! stdout .
stderr 'error\[config\.parse_error\]'
stderr 'Failed to parse rellog configuration file\.'

-- .rellog/config.kdl --
= invalid kdl content
