exec sh -c 'git init && git remote add origin https://github.com/tooppoo/rellog'


mkdir .rellog
mkdir .rellog/config.kdl

! exec rellog init
! stdout .
stderr 'Error: failed to create .rellog/config.kdl:'

