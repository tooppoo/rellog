skip pending: #22 contract, enable in #23

exec git init
! exec git remote get-url origin

exec rellog init
! stdout .
! stderr .

exists .rellog/config.kdl
! grep github-url .rellog/config.kdl
! grep rendering .rellog/config.kdl
