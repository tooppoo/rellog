skip 'legacy init required git repository before #23'

exec sh -c 'git init && git remote add origin https://github.com/tooppoo/rellog'

exec sh -c 'rm -rf .git'
! exec rellog init
! stdout .
stderr 'rellog init must be run inside a git repository'
! exists .rellog
